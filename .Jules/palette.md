## 2024-06-05 - Improving Accessibility with Focus-Visible
**Learning:** Adding `focus-visible` styles with `outline-offset` is a simple yet high-impact way to improve accessibility without affecting the visual design for mouse users. It provides a clear visual indicator for keyboard users while remaining invisible to mouse users.
**Action:** Always include `focus-visible` states for interactive elements in custom CSS systems to ensure keyboard navigability.
