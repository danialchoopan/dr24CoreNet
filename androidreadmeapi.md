# راهنمای توسعه اندروید برای پلتفرم dr24CoreNet

توسعه‌دهندگان کلاینت اندروید باید موارد زیر را در پیاده‌سازی خود لحاظ کنند:

## چرخه حیات رزرو نوبت
۱. **رزرو موقت**: ابتدا API رزرو را فراخوانی کنید. در صورت موفقیت، نوبت به مدت ۱۰ دقیقه برای کاربر رزرو می‌شود.
۲. **تایمر معکوس**: اپلیکیشن باید یک تایمر ۱۰ دقیقه‌ای نمایش دهد. در صورت عدم پرداخت در این زمان، نوبت از سمت سرور آزاد می‌شود.
۳. **تایید نهایی**: پس از کسر وجه از کیف پول (Wallet API)، رزرو قطعی می‌شود.

## سیستم ارجاع هوشمند
برای استفاده از تخفیف‌ها، توکن ارجاع باید در هدر درخواست رزرو نوبت ارسال شود:
- `X-Referral-Token`: [کد ۱۰ رقمی]

## دسترسی به سوابق و لاگ‌ها
تمامی فعالیت‌های کلاینت در سیستم Audit مرکزی ثبت می‌شود. برای حفظ حریم خصوصی، اپلیکیشن باید در هر درخواست IP واقعی کاربر را ارسال کند.

## تحلیل داده‌های محلی
پیشنهاد می‌شود برای نمایش نمودارها در اپلیکیشن اندروید، از داده‌های اسنپ‌شات (`/revenue-snapshot`) استفاده کنید تا حجم مصرف دیتای موبایل کاهش یابد.
