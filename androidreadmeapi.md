# راهنمای اتصال اپلیکیشن اندروید به dr24CoreNet

توسعه‌دهندگان اندروید باید موارد زیر را در پیاده‌سازی کلاینت رعایت کنند.

## مدیریت رزرو نوبت و خطای همزمانی
هنگام فراخوانی `/api/appointments/book` اگر با کد وضعیت `423 (Locked)` یا `409 (Conflict)` مواجه شدید، به این معنی است که پزشک همزمان توسط شخص دیگری در حال رزرو است. کاربر اندروید باید پیغام "نوبت در حال رزرو توسط دیگری است" را مشاهده کند.

## ارتباط زنده (Real-time)
برای بخش مشاوره آنلاین، از کتابخانه SignalR کلاینت اندروید استفاده کنید و به هاب `/chatHub` متصل شوید.

## کش سمت کلاینت
توصیه می‌شود لیست تخصص‌ها (Specializations) را به صورت محلی در دیتابیس Room اندروید کش کنید و هر ۲۴ ساعت بروزرسانی نمایید.
